import * as React from "react";
import { viewProps } from "../utils/interfaces";

export class DetailView extends React.Component<viewProps, {}> {
    render() {
        return <h2>Detail View</h2>;
    }
}